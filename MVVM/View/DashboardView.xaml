<UserControl x:Class="proyecto_tdp_2.MVVM.View.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:proyecto_tdp_2.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">


    <UserControl.DataContext>
        <vm:DashboardViewModel/>
    </UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>

            <TextBlock Text="Dashboard del Operador"
                       FontWeight="Bold"
                       FontSize="24"
                       Margin="0,0,0,20" />

            <UniformGrid Columns="4" Margin="0,0,0,20" HorizontalAlignment="Stretch" >
                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Total Reclamos" FontWeight="Bold" />
                        <TextBlock Text="{Binding TotalReclamos}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Reclamos Abiertos" FontWeight="Bold" />
                        <TextBlock Text="{Binding ReclamosAbiertos}" FontSize="24" FontWeight="Bold" Foreground="#0066FF" />
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Reclamos Cerrados" FontWeight="Bold" />
                        <TextBlock Text="{Binding ReclamosCerrados}" FontSize="24" FontWeight="Bold" Foreground="#00B86B" />
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Reclamos Pendientes" FontWeight="Bold" />
                        <TextBlock Text="{Binding ReclamosPendientes}" FontSize="24" FontWeight="Bold" Foreground="#FF8C00" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Reclamos por Provincia" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding ReclamosPorZona}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,3">
                                        <TextBlock Text="{Binding Key}" Width="150"/>
                                        <TextBlock Text="{Binding Value}" FontWeight="Bold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="Reclamos por Prioridad" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding ReclamosPorPrioridad}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,3">
                                        <TextBlock Text="{Binding Key}" Width="150"/>
                                        <TextBlock Text="{Binding Value}" FontWeight="Bold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="5">
                <StackPanel>
                    <TextBlock Text="Reclamos Recientes" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
                    <DataGrid ItemsSource="{Binding ReclamosRecientes}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              Background="White"
                              BorderBrush="#E0E0E0"
                              BorderThickness="1"
                              RowBackground="White"
                              AlternatingRowBackground="#FAFAFA"
                              GridLinesVisibility="None">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding IdReclamo}" Width="60" />
                            <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*" />
                            <DataGridTextColumn Header="Zona" Binding="{Binding Direccion}" Width="120" />
                            <DataGridTextColumn Header="Prioridad" Binding="{Binding Prioridad}" Width="100" />
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100" />
                            <DataGridTextColumn Header="Fecha" Binding="{Binding FechaCreacion, StringFormat='yyyy-MM-dd'}" Width="120" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
