<Window x:Class="proyecto_tdp_2.MVVM.View.AsignarOperadorView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:proyecto_tdp_2.MVVM.View"
    Title="MiReclamo - Asignar Operador"
    Height="700"
    Width="600"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent">

    <Border CornerRadius="20"
        Background="White"
        Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0"
                   Background="Transparent"
                   MouseDown="TopBar_MouseDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        DockPanel.Dock="Right"
                        Margin="0,5,10,0">
                    <Button Content="X"
                        Width="30"
                        Height="30"
                        Background="Transparent"
                        Foreground="Red"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Click="BtnExit_Click" />
                </StackPanel>
            </DockPanel>

            <StackPanel Grid.Row="1"
                    Margin="40"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <TextBlock Text="Asignar Operador"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,30"
                       HorizontalAlignment="Center" />

                <StackPanel Margin="0,10">
                    <TextBlock Text="Seleccionar Operador"
                           FontWeight="Bold" HorizontalAlignment="Center"/>
                    <ComboBox x:Name="comboOperadores" Margin="0,10,0,20" Foreground="Black"/>
                    <Button Content="Asignar"
                        Width="120"
                        Height="35"
                        Background="#0066FF"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="0"
                        FontWeight="Bold"
                        Click="BtnAsignar_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                CornerRadius="15"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
