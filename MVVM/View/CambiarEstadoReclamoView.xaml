<Window x:Class="proyecto_tdp_2.MVVM.View.CambiarEstadoReclamoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MiReclamo - Cambiar Estado"
        Height="550"
        Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="20"
            Background="White"
            Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0"
                       Background="Transparent"
                       MouseDown="TopBar_MouseDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            DockPanel.Dock="Right"
                            Margin="0,5,10,0">
                    <Button Content="X"
                            Width="30"
                            Height="30"
                            Background="Transparent"
                            Foreground="Red"
                            BorderThickness="0"
                            FontWeight="Bold"
                            Click="BtnExit_Click" />
                </StackPanel>
            </DockPanel>

            <StackPanel Grid.Row="1"
                        Margin="0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <TextBlock Text="Cambiar Estado del Reclamo"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,0,0,30"
                           HorizontalAlignment="Center" />

                <StackPanel Margin="0,10">
                    <TextBlock Text="Nuevo Estado"
                               Margin="12,0,0,10"
                               FontWeight="Bold" />
                    <ComboBox x:Name="CmbEstado"
                              Width="300"
                              Height="35"
                              BorderBrush="#3498DB"
                              BorderThickness="1"
                              VerticalContentAlignment="Center"
                              SelectedIndex="0">
                        <ComboBoxItem Content="Pendiente" />
                        <ComboBoxItem Content="En Curso" />
                        <ComboBoxItem Content="Resuelto" />
                        <ComboBoxItem Content="Cancelado" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,10">
                    <TextBlock Text="Comentario"
                               Margin="11,0,0,10"
                               FontWeight="Bold" />
                    <TextBox x:Name="TxtComentario"
                             Width="300"
                             Height="100"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             BorderBrush="#3498DB"
                             BorderThickness="1"
                             Padding="5"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,30,0,0">
                    <Button Content="Guardar"
                            Width="120"
                            Height="35"
                            Background="#0066FF"
                            Foreground="White"
                            BorderThickness="0"
                            Margin="0,0,20,0"
                            FontWeight="Bold"
                            Click="BtnGuardar_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="15"
                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Cancelar"
                            Width="120"
                            Height="35"
                            Background="Gray"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="Bold"
                            Click="BtnCancelar_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="15"
                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
